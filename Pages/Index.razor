@page "/"
@implements IAsyncDisposable

<div class="app-container">
    <header class="app-header">
        <div class="wordmark" aria-label="relexx Template Generator">
            <span class="wordmark-glyph">
                <svg viewBox="0 0 22 22" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <rect x="1" y="1" width="20" height="20" rx="2" stroke="currentColor" stroke-opacity="0.18" stroke-width="1"/>
                    <path d="M5 16 L5 6 L11 6 L11 11 L16 16" stroke="var(--color-accent)" stroke-width="1.6" stroke-linecap="square" stroke-linejoin="miter"/>
                    <rect x="14" y="4" width="3" height="3" fill="var(--color-accent)"/>
                </svg>
            </span>
            <span class="wordmark-text">
                <span class="accent">relexx</span><span>_tmpl</span><span class="cursor"></span>
            </span>
        </div>

        <div class="header-center">
            <span class="header-title">@(string.IsNullOrWhiteSpace(article.Title) ? "Neuer Artikel" : article.Title)</span>
            <span class="header-divider">│</span>
            <span class="header-layout">@currentLayout.Name</span>
        </div>

        <div class="stepper" role="tablist">
            @for (var i = 0; i <= 3; i++)
            {
                var phase = i;
                var isActive = currentPhase == phase;
                var isDone = phase < currentPhase || (_maxReached >= phase && phase != currentPhase);
                <button class="step @(isActive ? "is-active" : "") @(isDone && !isActive ? "is-done" : "")"
                        @onclick="async () => { if (phase <= _maxReached + 1) await GoToPhase(phase); }"
                        role="tab" aria-selected="@isActive" disabled="@(phase > _maxReached + 1)">
                    <span class="step-num">@phase</span>
                    @GetPhaseName(phase)
                </button>
                @if (i < 3)
                {
                    <span class="step-connector @(currentPhase > phase ? "is-done" : "")" aria-hidden="true"></span>
                }
            }
        </div>

        <div class="stepper-mobile" role="navigation">
            <button class="phase-nav-btn" @onclick="async () => { if (currentPhase > 0) await GoToPhase(currentPhase - 1); }"
                    disabled="@(currentPhase == 0)" aria-label="Zurück">&#8249;</button>
            <div class="phase-mobile-current">
                <span class="phase-mobile-num">@currentPhase</span>
                <span class="phase-mobile-label">@GetPhaseName(currentPhase)</span>
            </div>
            <button class="phase-nav-btn" @onclick="async () => { if (currentPhase < 3 && currentPhase < _maxReached + 1) await GoToPhase(currentPhase + 1); }"
                    disabled="@(currentPhase == 3 || currentPhase >= _maxReached + 1)" aria-label="Weiter">&#8250;</button>
        </div>

        <div style="position:relative; flex-shrink:0;">
            <button class="settings-btn @(_showSettings ? "open" : "")"
                    @onclick="() => _showSettings = !_showSettings"
                    title="Einstellungen">
                <Icon Name="settings" Size="16" />
            </button>
            <div class="settings-flyout @(_showSettings ? "open" : "")">
                <div class="settings-section">
                    <span class="settings-section-label">Theme</span>
                    <div class="settings-toggle-row">
                        <button class="settings-toggle-btn @(_theme == "dark" ? "active" : "")"
                                @onclick='async () => await SetTheme("dark")'>
                            <Icon Name="moon" Size="13" /> Dark
                        </button>
                        <button class="settings-toggle-btn @(_theme == "light" ? "active" : "")"
                                @onclick='async () => await SetTheme("light")'>
                            <Icon Name="sun" Size="13" /> Light
                        </button>
                    </div>
                </div>
                <div class="settings-section">
                    <span class="settings-section-label">Dichte</span>
                    <div class="settings-toggle-row">
                        <button class="settings-toggle-btn @(_density == "comfortable" ? "active" : "")"
                                @onclick='async () => await SetDensity("comfortable")'>
                            Komfort
                        </button>
                        <button class="settings-toggle-btn @(_density == "compact" ? "active" : "")"
                                @onclick='async () => await SetDensity("compact")'>
                            Kompakt
                        </button>
                    </div>
                </div>
                <div class="settings-section">
                    <span class="settings-section-label">Akzentfarbe</span>
                    <div class="accent-swatches">
                        @foreach (var preset in AccentPresets)
                        {
                            <button class="accent-swatch @(_accentPreset == preset.Key ? "active" : "")"
                                    style="background: @preset.Value.Hex"
                                    title="@preset.Key"
                                    @onclick="async () => await SetAccent(preset.Key)">
                            </button>
                        }
                    </div>
                </div>
            </div>
        </div>
    </header>

    <main class="app-main">
        @switch (currentPhase)
        {
            case 0: @RenderLayoutPhase() break;
            case 1: @RenderInputPhase()  break;
            case 2: @RenderPreviewPhase() break;
            case 3: @RenderHtmlPhase()   break;
        }
    </main>
</div>

@RenderDialogs()
@RenderNotification()

@code {
    private RenderFragment RenderLayoutPhase() => __builder =>
    {
        <div class="phase-content layout-phase">
            <div class="toolbar">
                <select class="layout-select" @bind="selectedLayoutId" @bind:after="OnLayoutSelected">
                    @foreach (var l in layouts)
                    {
                        <option value="@l.Id">@l.Name @(l.IsDefault ? "(Standard)" : "")</option>
                    }
                </select>
                <button class="btn btn-secondary" @onclick="CreateNewLayout" title="Neues Layout erstellen">
                    <span class="icon">➕</span> Neu
                </button>

                <InputFile id="layout-import" OnChange="ImportLayout" accept=".json" style="display:none" />

                <div class="toolbar-overflow-group">
                    <button class="btn btn-secondary toolbar-more-btn" @onclick="() => _showLayoutMore = !_showLayoutMore" title="Weitere Aktionen">
                        <span class="icon">•••</span>
                    </button>
                    <div class="toolbar-flyout @(_showLayoutMore ? "open" : "")">
                        <button class="btn btn-secondary" @onclick="async () => { _showLayoutMore = false; await DuplicateLayout(); }" title="Layout duplizieren">
                            <span class="icon">📋</span> Duplizieren
                        </button>
                        <button class="btn btn-secondary" @onclick="async () => { _showLayoutMore = false; await ExportLayout(); }" title="Layout exportieren">
                            <span class="icon">📤</span> Export
                        </button>
                        <button class="btn btn-secondary" title="Layout importieren"
                                @onclick='async () => { _showLayoutMore = false; await JS.InvokeVoidAsync("triggerClick", "layout-import"); }'>
                            <span class="icon">📥</span> Import
                        </button>
                        <button class="btn btn-danger" @onclick="async () => { _showLayoutMore = false; await DeleteLayout(); }"
                                disabled="@(currentLayout.IsDefault || layouts.Count <= 1)"
                                title="Layout löschen">
                            <span class="icon">🗑️</span> Löschen
                        </button>
                    </div>
                </div>

                <div class="toolbar-inline-group">
                    <button class="btn btn-secondary" @onclick="DuplicateLayout" title="Layout duplizieren">
                        <span class="icon">📋</span> Duplizieren
                    </button>
                    <button class="btn btn-secondary" @onclick="ExportLayout" title="Layout exportieren">
                        <span class="icon">📤</span> Export
                    </button>
                    <button class="btn btn-secondary" title="Layout importieren"
                            @onclick='() => JS.InvokeVoidAsync("triggerClick", "layout-import")'>
                        <span class="icon">📥</span> Import
                    </button>
                    <button class="btn btn-danger" @onclick="DeleteLayout"
                            disabled="@(currentLayout.IsDefault || layouts.Count <= 1)"
                            title="Layout löschen">
                        <span class="icon">🗑️</span>
                    </button>
                </div>
            </div>

            <div class="layout-editor">
                <div class="layout-sidebar">
                    <div class="sidebar-section">
                        <label>Layout-Name</label>
                        <input type="text" @bind="currentLayout.Name" @bind:after="SaveLayouts"
                               disabled="@currentLayout.IsDefault" placeholder="Layout-Name" />
                    </div>

                    <div class="sidebar-section">
                        <div class="section-header">
                            <h3>Blöcke</h3>
                            <button class="btn btn-small btn-accent" @onclick="() => showAddBlockDialog = true">
                                <span class="icon">➕</span>
                            </button>
                        </div>
                        <div class="block-list" id="block-list">
                            @foreach (var block in currentLayout.Blocks.OrderBy(b => b.Order))
                            {
                                <div @key="block.Id"
                                     class="block-item @(selectedBlockId == block.Id ? "selected" : "")"
                                     data-id="@block.Id"
                                     @onclick="() => SelectBlock(block)">
                                    <span class="drag-handle">☰</span>
                                    <span class="block-icon">@block.Icon</span>
                                    <span class="block-title">@block.Title</span>
                                    <span class="block-type">@block.Type.GetDisplayName()</span>
                                </div>
                            }
                        </div>
                    </div>

                    <div class="sidebar-section">
                        <h3>🎨 Farbschema</h3>
                        <div class="color-row">
                            <label>Primär</label>
                            <input type="color" @bind="currentLayout.Colors.PrimaryColor" @bind:after="SaveLayouts" />
                            <input type="text" class="color-text" @bind="currentLayout.Colors.PrimaryColor" @bind:after="SaveLayouts" />
                        </div>
                        <div class="color-row">
                            <label>Akzent</label>
                            <input type="color" @bind="currentLayout.Colors.AccentColor" @bind:after="SaveLayouts" />
                            <input type="text" class="color-text" @bind="currentLayout.Colors.AccentColor" @bind:after="SaveLayouts" />
                        </div>
                        <div class="color-row">
                            <label>Hintergrund</label>
                            <input type="color" @bind="currentLayout.Colors.BackgroundColor" @bind:after="SaveLayouts" />
                            <input type="text" class="color-text" @bind="currentLayout.Colors.BackgroundColor" @bind:after="SaveLayouts" />
                        </div>
                    </div>
                </div>

                <div class="block-editor">
                    @if (SelectedBlock is not null)
                    {
                        <h3>Block bearbeiten</h3>

                        <div class="editor-group">
                            <label>Id</label>
                            <input type="text" value="@SelectedBlock.Id"
                                   class="@(!string.IsNullOrEmpty(_blockIdError) ? "input-error" : string.Empty)"
                                   @onchange="async e => await ChangeBlockId(SelectedBlock, e.Value?.ToString() ?? string.Empty)"
                                   placeholder="block-id" />
                            @if (!string.IsNullOrEmpty(_blockIdError))
                            {
                                <span class="field-error">@_blockIdError</span>
                            }
                            <small class="hint">Eindeutiger Bezeichner, wird für JSON-Export verwendet.</small>
                        </div>

                        <div class="editor-group">
                            <label>Block-Typ</label>
                            <select @bind="SelectedBlock.Type" @bind:after="OnBlockTypeChanged">
                                @foreach (BlockType t in Enum.GetValues<BlockType>())
                                {
                                    <option value="@t">@t.GetDisplayName()</option>
                                }
                            </select>
                        </div>

                        <div class="editor-group">
                            <label>Icon</label>
                            <div class="icon-picker">
                                @foreach (var icon in Constants.AvailableIcons)
                                {
                                    <button type="button" class="icon-btn @(SelectedBlock.Icon == icon ? "selected" : "")"
                                            @onclick="() => SetBlockIcon(icon)">@icon</button>
                                }
                            </div>
                        </div>

                        <div class="editor-group">
                            <label>Überschrift</label>
                            <input type="text" @bind="SelectedBlock.Title" @bind:after="SaveLayouts" />
                        </div>

                        @RenderBlockOptions(SelectedBlock)

                        <div class="editor-actions">
                            <button class="btn btn-danger" @onclick="DeleteBlock">
                                <span class="icon">🗑️</span> Block löschen
                            </button>
                        </div>
                    }
                    else
                    {
                        <div class="no-selection">
                            <p>Wähle einen Block aus der Liste.</p>
                            <p>Ziehe Blöcke per Drag & Drop, um die Reihenfolge zu ändern.</p>
                        </div>
                    }
                </div>
            </div>

            <div class="nav-buttons">
                <div></div>
                <button class="btn btn-primary btn-large" @onclick="() => GoToPhase(1)">
                    Weiter zur Eingabe <span class="icon">→</span>
                </button>
            </div>
        </div>
    };

    private RenderFragment RenderBlockOptions(BlockDefinition block) => __builder =>
    {
        <div class="editor-group checkbox-group">
            <label>
                <input type="checkbox" @bind="block.Options.ShowTitle" @bind:after="SaveLayouts" />
                Überschrift im Artikel anzeigen
            </label>
        </div>

        @if (block.Type == BlockType.KeyValueGrid)
        {
            <div class="editor-group">
                <label>Spalten</label>
                <select @bind="block.Options.Columns" @bind:after="SaveLayouts">
                    <option value="1">1 Spalte</option>
                    <option value="2">2 Spalten</option>
                    <option value="3">3 Spalten</option>
                </select>
            </div>
            @RenderBulletPicker(block)
        }

        @if (block.Type == BlockType.DataTable)
        {
            <div class="editor-group">
                <label>Spalte 1</label>
                <input type="text" @bind="block.Options.Column1Header" @bind:after="SaveLayouts" />
            </div>
            <div class="editor-group">
                <label>Spalte 2</label>
                <input type="text" @bind="block.Options.Column2Header" @bind:after="SaveLayouts" />
            </div>
            <div class="editor-group checkbox-group">
                <label>
                    <input type="checkbox" @bind="block.Options.ShowColumnHeaders" @bind:after="SaveLayouts" />
                    Spaltenüberschriften
                </label>
            </div>
            <div class="editor-group checkbox-group">
                <label>
                    <input type="checkbox" @bind="block.Options.AlternatingBackground" @bind:after="SaveLayouts" />
                    Zebra-Streifen
                </label>
            </div>
        }

        @if (block.Type == BlockType.CheckList)
        {
            @RenderBulletPicker(block)
        }

        @if (block.Type == BlockType.Image)
        {
            <div class="editor-group">
                <label>Ausrichtung</label>
                <select @bind="block.Options.Alignment" @bind:after="SaveLayouts">
                    <option value="left">Links</option>
                    <option value="center">Zentriert</option>
                    <option value="right">Rechts</option>
                </select>
            </div>
            <div class="editor-group">
                <label>Max. Breite (px)</label>
                <input type="number" @bind="block.Options.MaxWidth" @bind:after="SaveLayouts" min="100" max="800" />
            </div>
        }

        @if (block.Type == BlockType.FixedText)
        {
            <div class="editor-group">
                <label>Vorlagen-Text (Markdown)</label>
                <textarea rows="6" @bind="block.Options.FixedContent" @bind:after="() => OnFixedContentChanged(block)"
                          placeholder="Fester Text, der im Layout gespeichert wird..."></textarea>
                <small class="hint">Dieser Text wird als Standardwert für neue Artikel verwendet.</small>
            </div>
        }
    };

    private RenderFragment RenderBulletPicker(BlockDefinition block) => __builder =>
    {
        <div class="editor-group">
            <label>Aufzählungszeichen</label>
            <div class="bullet-picker">
                @foreach (var b in Constants.AvailableBullets)
                {
                    <button type="button" class="bullet-btn @(block.Options.BulletChar == b ? "selected" : "")"
                            @onclick="() => SetBullet(b)">@b</button>
                }
            </div>
        </div>
    };

    private RenderFragment RenderInputPhase() => __builder =>
    {
        <div class="phase-content input-phase">
            <div class="toolbar">
                <button class="btn btn-secondary" @onclick="LoadDemo">
                    <span class="icon">📋</span> Demo
                </button>
                <button class="btn btn-secondary" @onclick="ResetArticle">
                    <span class="icon">🗑️</span> Zurücksetzen
                </button>

                <InputFile id="article-import" OnChange="ImportArticle" accept=".json" style="display:none" />

                <div class="toolbar-overflow-group">
                    <button class="btn btn-secondary toolbar-more-btn" @onclick="() => _showInputMore = !_showInputMore" title="Weitere Aktionen">
                        <span class="icon">•••</span>
                    </button>
                    <div class="toolbar-flyout @(_showInputMore ? "open" : "")">
                        <button class="btn btn-secondary" @onclick="async () => { _showInputMore = false; await CopyJsonSchema(); }">
                            <span class="icon">🤖</span> JSON-Schema
                        </button>
                        <button class="btn btn-secondary" @onclick="async () => { _showInputMore = false; await ExportArticle(); }">
                            <span class="icon">📤</span> Export
                        </button>
                        <button class="btn btn-secondary"
                                @onclick='async () => { _showInputMore = false; await JS.InvokeVoidAsync("triggerClick", "article-import"); }'>
                            <span class="icon">📥</span> Import
                        </button>
                    </div>
                </div>

                <div class="toolbar-inline-group">
                    <button class="btn btn-secondary" @onclick="CopyJsonSchema">
                        <span class="icon">🤖</span> JSON-Schema
                    </button>
                    <button class="btn btn-secondary" @onclick="ExportArticle">
                        <span class="icon">📤</span> Export
                    </button>
                    <button class="btn btn-secondary"
                            @onclick='() => JS.InvokeVoidAsync("triggerClick", "article-import")'>
                        <span class="icon">📥</span> Import
                    </button>
                </div>
            </div>

            <div class="form-grid">
                <div class="form-section">
                    <h2>📌 Header</h2>
                    <div class="form-group">
                        <label>Titel</label>
                        <input type="text" @bind="article.Title" placeholder="Produktname" />
                    </div>
                    <div class="form-group">
                        <label>Untertitel</label>
                        <input type="text" @bind="article.Subtitle" placeholder="Kurzbeschreibung │ Artikelnummer" />
                    </div>
                </div>

                <div class="form-section">
                    <h2>📱 Mobile Summary</h2>
                    <div class="form-group">
                        <label>Zusammenfassung (max. @Constants.Limits.MaxMobileSummaryLength Zeichen)</label>
                        <textarea @bind="article.MobileSummary" rows="3"
                                  placeholder="Kurze Produktbeschreibung für Mobile..."></textarea>
                        <span class="char-count @(article.MobileSummary?.Length > Constants.Limits.MaxMobileSummaryLength ? "over-limit" : "")">
                            @(article.MobileSummary?.Length ?? 0) / @Constants.Limits.MaxMobileSummaryLength
                        </span>
                    </div>
                </div>

                @foreach (var block in currentLayout.Blocks.OrderBy(b => b.Order))
                {
                    <div class="form-section">
                        <div class="section-header">
                            <h2>@block.Icon @block.Title</h2>
                            <button class="btn-clear" @onclick="() => ClearBlock(block.Id)" title="Inhalt löschen">🗑️</button>
                        </div>
                        <div class="form-group">
                            <label>@block.Type.GetInputLabel()</label>
                            @if (block.Type == BlockType.Image)
                            {
                                <div class="input-with-button">
                                    <input type="text" value="@article.GetBlockContent(block.Id)"
                                           @onchange="e => SetContent(block.Id, e)" placeholder="https://..." />
                                    <InputFile id="@($"img-{block.Id}")" OnChange="e => UploadImage(e, block.Id)" accept="image/*" style="display:none" />
                                    <button class="btn btn-accent" @onclick="() => TriggerImagePicker(block.Id)">
                                        <span class="icon">📷</span>
                                    </button>
                                </div>
                                @if (!string.IsNullOrEmpty(article.GetBlockContent(block.Id)))
                                {
                                    <div class="image-preview">
                                        <img src="@article.GetBlockContent(block.Id)" alt="Vorschau" />
                                    </div>
                                }
                            }
                            else
                            {
                                <textarea value="@article.GetBlockContent(block.Id)"
                                          @onchange="e => SetContent(block.Id, e)"
                                          rows="@GetTextareaRows(block.Type)"
                                          placeholder="@block.Type.GetInputPlaceholder()"></textarea>
                            }
                        </div>
                    </div>
                }

                <div class="form-section">
                    <div class="section-header">
                        <h2>🏷️ Fußzeile</h2>
                        <button class="btn-clear" @onclick="() => article.Footer = string.Empty" title="Inhalt löschen">🗑️</button>
                    </div>
                    <div class="form-group">
                        <label>Pro Zeile ein Eintrag, max. @Constants.Limits.MaxFooterLines Zeilen (Format: Label: Wert)</label>
                        <textarea @bind="article.Footer" rows="4"
                                  placeholder="SKU: ABC123&#10;Farbe: Schwarz&#10;Garantie: 2 Jahre"></textarea>
                        <span class="char-count @(GetFooterLines() > Constants.Limits.MaxFooterLines ? "over-limit" : "")">
                            @GetFooterLines() / @Constants.Limits.MaxFooterLines Zeilen
                        </span>
                    </div>
                </div>
            </div>

            <div class="nav-buttons">
                <button class="btn btn-secondary btn-large" @onclick="() => GoToPhase(0)">
                    <span class="icon">←</span> Layout
                </button>
                <button class="btn btn-primary btn-large" @onclick="() => GoToPhase(2)">
                    Vorschau <span class="icon">→</span>
                </button>
            </div>
        </div>
    };

    private RenderFragment RenderPreviewPhase() => __builder =>
    {
        <div class="phase-content preview-phase">
            <div class="preview-container">
                <div class="preview-frame">
                    @((MarkupString)generatedHtml)
                </div>
            </div>

            <div class="nav-buttons">
                <button class="btn btn-secondary btn-large" @onclick="() => GoToPhase(1)">
                    <span class="icon">←</span> Eingabe
                </button>
                <button class="btn btn-primary btn-large" @onclick="() => GoToPhase(3)">
                    HTML <span class="icon">→</span>
                </button>
            </div>
        </div>
    };

    private RenderFragment RenderHtmlPhase() => __builder =>
    {
        <div class="phase-content html-phase">
            <div class="html-container">
                <div class="html-toolbar">
                    <button class="btn btn-primary" @onclick="CopyHtml">
                        <span class="icon">📋</span> Kopieren
                    </button>
                    <button class="btn btn-accent" @onclick="DownloadHtml">
                        <span class="icon">💾</span> Herunterladen
                    </button>
                </div>
                <pre class="html-code"><code>@generatedHtml</code></pre>
            </div>

            <div class="nav-buttons">
                <button class="btn btn-secondary btn-large" @onclick="() => GoToPhase(2)">
                    <span class="icon">←</span> Vorschau
                </button>
                <button class="btn btn-secondary btn-large" @onclick="() => GoToPhase(1)">
                    <span class="icon">🔄</span> Neuer Artikel
                </button>
            </div>
        </div>
    };

    private RenderFragment RenderDialogs() => __builder =>
    {
        @if (showAddBlockDialog)
        {
            <div class="modal-overlay" @onclick="() => showAddBlockDialog = false">
                <div class="modal-dialog" @onclick:stopPropagation>
                    <h3>Block hinzufügen</h3>
                    <div class="block-type-list">
                        @foreach (BlockType t in Enum.GetValues<BlockType>())
                        {
                            <button class="block-type-btn" @onclick="() => AddBlock(t)">
                                <span class="icon">@t.GetDefaultIcon()</span>
                                <span class="name">@t.GetDisplayName()</span>
                            </button>
                        }
                    </div>
                    <button class="btn btn-secondary" @onclick="() => showAddBlockDialog = false">Abbrechen</button>
                </div>
            </div>
        }

        @if (showImportConflict)
        {
            <div class="modal-overlay" @onclick="() => showImportConflict = false">
                <div class="modal-dialog" @onclick:stopPropagation>
                    <h3>⚠️ Layout-Konflikt</h3>
                    <p>Das importierte Layout unterscheidet sich vom aktuellen.</p>
                    <p><strong>Aktuell:</strong> @currentLayout.Name (@currentLayout.Blocks.Count Blöcke)</p>
                    <p><strong>Import:</strong> @importedArticle?.Layout?.Name (@importedArticle?.Layout?.Blocks.Count Blöcke)</p>

                    <div class="modal-actions">
                        <button class="btn btn-primary" @onclick="ImportWithNewLayout">Layout aus Datei übernehmen</button>
                        <button class="btn btn-secondary" @onclick="ImportKeepLayout">Versuchen, Daten einzupassen</button>
                        <button class="btn btn-secondary" @onclick="() => showImportConflict = false">Abbrechen</button>
                    </div>
                </div>
            </div>
        }
    };

    private RenderFragment RenderNotification() => __builder =>
    {
        @if (!string.IsNullOrEmpty(notification))
        {
            <div class="notification @(notificationSuccess ? "success" : "error")">@notification</div>
        }
    };
}
